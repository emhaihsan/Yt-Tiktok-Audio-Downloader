import yt_dlp
from pathlib import Path

def download_audio(url, output_dir):
    """Download audio from TikTok/Instagram/YouTube"""
    ydl_opts = {
        'format': 'bestaudio/best',
        'postprocessors': [{
            'key': 'FFmpegExtractAudio',
            'preferredcodec': 'mp3',
            'preferredquality': '192',
        }],
        'outtmpl': str(output_dir / '%(title)s.%(ext)s'),
    }

    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        info = ydl.extract_info(url, download=True)
        title = info['title']
        filename = f"{title}.mp3"
        filepath = output_dir / filename

    return filepath, title

def download_audio_for_split(url, output_dir, prefix="split"):
    """Download audio with prefix for split tab"""
    ydl_opts = {
        'format': 'bestaudio/best',
        'postprocessors': [{
            'key': 'FFmpegExtractAudio',
            'preferredcodec': 'mp3',
            'preferredquality': '192',
        }],
        'outtmpl': str(output_dir / f'{prefix}_%(title)s.%(ext)s'),
    }

    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        info = ydl.extract_info(url, download=True)
        title = info['title']
        filename = f"{prefix}_{title}.mp3"
        filepath = output_dir / filename

    return filepath, title
